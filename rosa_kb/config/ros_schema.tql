define

package sub attribute,
    value string;

executable sub attribute,
    value string;

ROSNode sub Component,
    owns package,
    owns executable;

LifeCycleNode sub ROSNode;

system-mode sub function-design;
node-mode sub component-configuration;

#system-mode-adaptation sub relation,
#    relates system-mode,
#    plays reconfiguration-plan:structural-adaptation;
